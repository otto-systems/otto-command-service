{
  "name": "gateway.sync.all",
  "description": "Run unified provider sync orchestration.",
  "parameters": {
    "type": "object",
    "required": [],
    "additionalProperties": false,
    "properties": {
      "factsCsv": { "type": "string" },
      "calendarId": { "type": "string" },
      "userId": { "type": "string" },
      "playerId": { "type": "string" },
      "playlistUrl": { "type": "string" }
    }
  },
  "returnType": { "type": "object", "additionalProperties": true },
  "errorTypes": ["GatewaySyncAllError"],
  "permissions": ["gateway:sync"],
  "routing": {
    "handlerModule": "gatewaySyncAll.mjs",
    "handlerExport": "handle",
    "exposedAs": ["cli", "api"],
    "http": { "method": "POST", "path": "/v1/commands/gateway/sync-all" },
    "cli": { "group": "gateway", "command": "sync-all" },
    "operationId": "gatewaySyncAll"
  }
}
