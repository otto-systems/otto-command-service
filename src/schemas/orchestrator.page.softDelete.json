{
  "name": "orchestrator.page.softDelete",
  "description": "Soft delete a page for a display.",
  "parameters": {
    "type": "object",
    "required": ["pageId"],
    "additionalProperties": true,
    "properties": {
      "displayId": { "type": "string" },
      "pageId": { "type": "string" }
    }
  },
  "returnType": { "type": "object", "additionalProperties": true },
  "errorTypes": ["OrchestratorPageDeleteError"],
  "permissions": ["display:write"],
  "routing": {
    "handlerModule": "orchestratorPageSoftDelete.mjs",
    "handlerExport": "handle",
    "exposedAs": ["cli", "api"],
    "http": { "method": "POST", "path": "/v1/commands/orchestrator/page/soft-delete" },
    "cli": { "group": "orchestrator", "command": "page-soft-delete" },
    "operationId": "orchestratorPageSoftDelete"
  }
}
